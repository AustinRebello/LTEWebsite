<!-- TOADS COMMAND HERE -->
<div class="container large page">
<%= render "common/results" %>
</div>

<% if user_signed_in? && current_user.admin? %>
<div class="container large page">
  <p style="color: green"><%= notice %></p>

  <%= render @misc %>

  <div>
    <%= link_to "Edit this misc", edit_misc_path(@misc) %> |
    <%= link_to "Back to miscs", miscs_path %>

    <%= button_to "Destroy this misc", @misc, method: :delete %>
  </div>
</div>
<% end %>
